[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Cron Job настраивается в Railway UI:
# Schedule: 0 * * * * (каждый час)
# Endpoint: POST /api/cron
